spring:
  application:
    ## 指定服务名称，在nacos中的名字
    name: xiaobai-up-rbac
  profiles:
    active: test
management:
  endpoints:
    web:
      exposure:
        ## yml文件中存在特殊字符，必须用单引号包含，否则启动报错
        include: '*'